Для работы с оценками необходимо инициализировать RuStoreReviewManager.URuStoreReviewManager::Instance()->Init();Пример использования в Blueprints:Parsed Image TextParsed Image Textк сведениюВызов Init() привязывает объект к корню сцены, и, если дальнейшая работа с объектом больше не планируется, для освобождения памяти необходимо выполнить метод Dispose().
Вызов метода Dispose отвяжет объект от корня и безопасно завершит все отправленные запросы.URuStoreReviewManager::Instance()->Dispose();Parsed Image TextЕсли вам нужно проверить факт инициализации библиотеки, используйте метод getIsInitialized(), его значение true, если библиотека инициализирована, и false, если Init еще не был вызван.Parsed Image Text